name: React Doctor

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  react-doctor:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          fetch-depth: 0

      - name: Scan changed React files
        if: github.event_name == 'pull_request'
        uses: millionco/react-doctor@a6f16d429679c1641b2d358f03438cdca9d670ba
        with:
          diff: main
          github-token: ${{ github.event.pull_request.head.repo.full_name == github.repository && secrets.GITHUB_TOKEN || '' }}
          fail-on: none
          offline: true
          verbose: false

      - name: Scan React project
        if: github.event_name != 'pull_request'
        uses: millionco/react-doctor@a6f16d429679c1641b2d358f03438cdca9d670ba
        with:
          fail-on: none
          offline: true
          verbose: false
